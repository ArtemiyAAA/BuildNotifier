<idea-plugin>
    <id>com.jetbrains.rider.buildnotifier</id>
    <name>BuildNotifier</name>
    <vendor url="https://www.jetbrains.com">JetBrains</vendor>

    <!-- Product and plugin compatibility requirements -->
    <!-- https://www.jetbrains.org/intellij/sdk/docs/basics/getting_started/plugin_compatibility.html -->
    <depends>com.intellij.modules.rider</depends>
    <depends>com.jetbrains.rider-cpp</depends>

    <!-- Plugin description excerpt from README.md, displayed on the Plugin's page in the Marketplace -->
    <!-- https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html#idea-plugin__description -->
    <!-- ATTENTION: Add HTML formatting only if really necessary so that users prefer to use it as is. Add links only if you need to know about other resources. Preserve an empty line between tags. -->
    <!-- ATTENTION: It is not acceptable to use HTML tags which are not supported (see https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html#idea-plugin__description). -->
    <description><![CDATA[
      BuildNotifier sends IDE balloon notifications and/or webhook calls when a build finishes in Rider.<br>
      Supports filtering by build duration and customizable webhook payload templates.
    ]]></description>

    <extensions defaultExtensionNs="com.intellij">
        <applicationConfigurable
            parentId="tools"
            instance="com.jetbrains.rider.buildnotifier.settings.AppSettingsConfigurable"
            id="com.jetbrains.rider.buildnotifier.settings.AppSettingsConfigurable"
            displayName="BuildNotifier" />
        <applicationService serviceImplementation="com.jetbrains.rider.buildnotifier.settings.AppSettingsState" />
        <notificationGroup id="BuildNotifier.Notification" displayType="BALLOON" bundle="messages.BuildNotifierBundle" key="BuildNotifier.notification.group.name"/>
        <postStartupActivity implementation="com.jetbrains.rider.buildnotifier.services.BuildNotificationStartupActivity"/>
    </extensions>
</idea-plugin>
